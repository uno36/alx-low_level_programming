#include "main.h"
/**
 * _abs - function to display the absolute value of a number
 * Return: retune (-r) if negative int or return (r) is negative int
 * @r : param for function _abs()
*/
int _abs(int r)
{
	if (r < 0)
	return (-r);
	else
	return (r);

}
