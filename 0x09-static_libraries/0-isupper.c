#include "main.h"
/**
 * _isupper - function to return 1 if uppercase
 * Return: returns some parameters
 * @c : argument
*/
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
	return (1);

	else
	return (0);
}
